# Environment variables for Day 33 ISS Tracker
LAT=40.7128              # Your latitude (e.g., New York City)
LNG=-74.0060             # Your longitude
EMAIL_USER=
EMAIL_PASS=
TO_EMAIL=                 # Where to send notifications (defaults to EMAIL_USER if blank)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
USE_TLS=true
DRY_RUN=true              # Set false to actually send email
FROM_NAME=ISS Notifier
FORCE_NOTIFY=false        # true to bypass overhead + night checks (for testing)
CHECK_INTERVAL=600        # seconds between checks if looping
DISTANCE_DEG_THRESHOLD=5  # angular distance threshold in degrees for "overhead"
